import { types } from "../types";




export const UserContextReducer = (state, action)=> {
    const {type, payload} = action;


    switch (type) {
        
    
        default:
            return state;
    }
}